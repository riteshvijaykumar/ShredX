# HDD Tool Server Configuration

## Server Settings
PORT=3000
HOST=0.0.0.0

## Database Settings (PostgreSQL)
DATABASE_URL=postgresql://hdd_user:root@localhost/hdd_tool_db
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hdd_tool_db
DB_USER=hdd_user
DB_PASSWORD=root

## Authentication Settings
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-openssl-rand-base64-32
JWT_EXPIRATION_HOURS=24

## Logging Settings
RUST_LOG=info
LOG_LEVEL=info

## Security Settings
ENABLE_CORS=true
MAX_REQUEST_SIZE=10485760  # 10MB
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60  # seconds

## Hardware Settings
DRIVE_SCAN_INTERVAL=30  # seconds
MAX_CONCURRENT_JOBS=3
DEFAULT_SANITIZATION_METHOD=zeros

## File Upload Settings (for future use)
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE=104857600  # 100MB